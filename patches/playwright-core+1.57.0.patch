diff --git a/node_modules/playwright-core/lib/server/electron/electron.js b/node_modules/playwright-core/lib/server/electron/electron.js
index 3fb4963..3b6c7c2 100644
--- a/node_modules/playwright-core/lib/server/electron/electron.js
+++ b/node_modules/playwright-core/lib/server/electron/electron.js
@@ -52,6 +52,15 @@ var js = __toESM(require("../javascript"));
 var import_processLauncher = require("../utils/processLauncher");
 var import_transport = require("../transport");
 const ARTIFACTS_FOLDER = import_path.default.join(import_os.default.tmpdir(), "playwright-artifacts-");
+function playwrightElectronRemoteDebuggingPortSpec() {
+  const envPort = process.env.PW_ELECTRON_DEBUG_PORT;
+  if (envPort) {
+    const n = Number(envPort);
+    if (Number.isFinite(n) && n >= 0 && n <= 65535)
+      return String(n);
+  }
+  return "0";
+}
 class ElectronApplication extends import_instrumentation.SdkObject {
   constructor(parent, browser, nodeConnection, process2) {
     super(parent, "electron-app");
@@ -129,7 +138,8 @@ class Electron extends import_instrumentation.SdkObject {
   }
   async launch(progress, options) {
     let app = void 0;
-    let electronArguments = ["--inspect=0", "--remote-debugging-port=0", ...options.args || []];
+    const remoteDebuggingPortSpec = playwrightElectronRemoteDebuggingPortSpec();
+    let electronArguments = ["--inspect=0", ...options.args || []];
     if (import_os.default.platform() === "linux") {
       const runningAsRoot = process.geteuid && process.geteuid() === 0;
       if (runningAsRoot && electronArguments.indexOf("--no-sandbox") === -1)
@@ -137,7 +147,8 @@ class Electron extends import_instrumentation.SdkObject {
     }
     const artifactsDir = await progress.race(import_fs.default.promises.mkdtemp(ARTIFACTS_FOLDER));
     const browserLogsCollector = new import_debugLogger.RecentLogsCollector();
-    const env = options.env ? (0, import_processLauncher.envArrayToObject)(options.env) : process.env;
+    let env = options.env ? (0, import_processLauncher.envArrayToObject)(options.env) : { ...process.env };
+    env.PW_ELECTRON_REMOTE_DEBUGGING_PORT = remoteDebuggingPortSpec;
     let command;
     if (options.executablePath) {
       command = options.executablePath;
diff --git a/node_modules/playwright-core/lib/server/electron/loader.js b/node_modules/playwright-core/lib/server/electron/loader.js
index b6e9708..b2a1fb3 100644
--- a/node_modules/playwright-core/lib/server/electron/loader.js
+++ b/node_modules/playwright-core/lib/server/electron/loader.js
@@ -1,7 +1,14 @@
 "use strict";
 const { app } = require("electron");
 const { chromiumSwitches } = require("../chromium/chromiumSwitches");
-process.argv.splice(1, process.argv.indexOf("--remote-debugging-port=0"));
+const remotePort = process.env.PW_ELECTRON_REMOTE_DEBUGGING_PORT;
+if (remotePort)
+  app.commandLine.appendSwitch("remote-debugging-port", remotePort);
+const remoteDebuggingArgIndex = process.argv.findIndex(
+  (arg) => typeof arg === "string" && arg.startsWith("--remote-debugging-port=")
+);
+if (remoteDebuggingArgIndex !== -1)
+  process.argv.splice(1, remoteDebuggingArgIndex);
 for (const arg of chromiumSwitches()) {
   const match = arg.match(/--([^=]*)=?(.*)/);
   app.commandLine.appendSwitch(match[1], match[2]);
